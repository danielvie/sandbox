export function addNumbers(a: number, b: number): number {
  return a + b;
}

export function multiplyNumbers(a: number, b: number): number {
  return a * b;
}

export const i_am_from_outside = () => {
    console.log('i am from outside')
}