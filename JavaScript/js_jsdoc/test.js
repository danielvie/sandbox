
/**
 * 
 * @param {number} a - first parameter
 * @param {number} b - second parameter
 * @returns {number}
 */
function soma(a, b) {
    return a + b;
}

/**
 * Returns first item
 * @param {Array} A 
 * @returns {number} comentario que acho que nao vai mudar nada 
 */
function first_item(A) {
    return A[0]
}

r = soma(a, b)

b = first_item(A)
b.

console.log(r)